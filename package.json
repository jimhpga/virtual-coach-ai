{
  "name": "virtual-coach-ai",
  "private": true,
  "scripts": {
    "build": "echo \"no build (static)\"",
    "start": "node -e \"console.log('local dev uses vercel dev; nothing to start')\""
  },
  "dependencies": {
    "@mux/mux-node": "^8.5.0"
  }
}
