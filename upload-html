<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Upload Your Swing - Virtual Coach AI</title>
  <link rel="stylesheet" href="style.css" />
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background-color: #f3f3f3;
      color: #222;
    }
    .topbar {
      background-color: #004d26;
      color: white;
      padding: 15px;
      text-align: center;
      font-size: 1.2em;
    }
    .topbar a {
      color: white;
      text-decoration: none;
      margin-right: 20px;
    }
    .container {
      max-width: 700px;
      margin: 40px auto;
      padding: 30px;
      background-color: white;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      text-align: center;
    }
    input[type="file"] {
      margin-top: 20px;
    }
    button {
      margin-top: 20px;
      padding: 10px 20px;
      background-color: #004d26;
      color: white;
      border: none;
      border-radius: 5px;
      font-size: 1em;
      cursor: pointer;
    }
    button:hover {
      background-color: #006633;
    }
  </style>
</head>
<body>
  <div class="topbar">
    <a href="index.html">Home</a>
    <a href="about.html">About</a>
    <a href="upload.html" style="text-decoration: underline;">Upload</a>
    <a href="coming-soon.html">Coming Soon</a>
  </div>

  <div class="container">
    <h1>Upload Your Swing</h1>
    <p>Select a swing video to upload. Supported formats: .mp4, .mov</p>
    <input type="file" id="fileInput" accept="video/*" />
    <br />
    <button onclick="uploadVideo()">Upload Video</button>
    <p id="status"></p>
  </div>

  <script>
    function uploadVideo() {
      const input = document.getElementById('fileInput');
      const file = input.files[0];
      if (!file) {
        alert("Please select a file first.");
        return;
      }
      document.getElementById("status").textContent = "Upload started... (Functionality coming soon)";
    }
  </script>
</body>
</html>
