import { redirect } from "next/navigation";

export default function Page({ searchParams }: { searchParams: any }) {
  if (!searchParams?.ok) {
    const params = new URLSearchParams(searchParams || {});
    params.set("ok", "1");
    redirect(/report-beta?\);
  }
  return null;
}
