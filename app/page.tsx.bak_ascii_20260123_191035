import Link from "next/link";

export default function Page() {
  return (
    <main style={{ padding: 28, fontFamily: "system-ui, -apple-system, Segoe UI, Roboto, Arial" }}>
      <h1 style={{ fontSize: 30, margin: 0 }}>Virtual Coach AI</h1>
      <p style={{ marginTop: 10, opacity: 0.85, maxWidth: 760 }}>
        Upload a swing, get a clean report (P1–P9, faults, drills) and a clear score. Simple.
      </p>

      <div style={{ display: "flex", gap: 12, marginTop: 18, flexWrap: "wrap" }}>
        <Link
          href="/upload"
          style={{
            padding: "12px 16px",
            borderRadius: 14,
            border: "1px solid rgba(255,255,255,0.22)",
            fontWeight: 700,
            textDecoration: "none",
          }}
        >
          Start Upload →
        </Link>

        <Link
          href="/report-beta"
          style={{
            padding: "12px 16px",
            borderRadius: 14,
            border: "1px solid rgba(255,255,255,0.22)",
            textDecoration: "none",
          }}
        >
          View Report Beta →
        </Link>
      </div>

      <div style={{ marginTop: 18, fontSize: 12, opacity: 0.7 }}>
        Tip: If you want upload-first later, we can re-add a redirect in middleware or config—without breaking anything.
      </div>
    </main>
  );
}
